```{r}
cat(strrep("-", 80), sep = "", file = stderr())
options(rlang_backtrace_on_error = "full")
```

```{r}
cat("👍\n", file = stderr())
f <- function() g()
g <- function() h()
h <- function() i()
i <- function() stop("!")
```

```{r}
f()
```
